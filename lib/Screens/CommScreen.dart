import 'package:flutter/material.dart';

class CommScreen extends StatelessWidget {
  const CommScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(child: Text("Comms"));
  }
}
